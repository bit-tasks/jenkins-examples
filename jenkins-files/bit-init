pipeline {
  agent {
    label 'docker' 
  }
  stages {
    stage('Build') {
      agent {
        docker {
          // Set both label and image
          label 'docker'
          image 'bitsrc/stable:latest'
        }
      }
      steps {
        sh 'bit -v'
      }
    }
  }
} 
